import { userRepository } from "src/repositories/userRepository";

class AuthController {
  register() {
    console.log("no register de user");
  }

  login() {
    console.log("no login de user");
  }
}

export default new AuthController();
